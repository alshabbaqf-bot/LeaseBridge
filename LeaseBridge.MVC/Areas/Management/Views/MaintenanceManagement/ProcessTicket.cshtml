@model LeaseBridge.MVC.Areas.Management.Controllers.ManagementProcessTicketViewModel

@{
    ViewData["Title"] = "Process Maintenance Request";
}

<div class="container page-container">
    <div class="card-custom">
        <div class="card-header-navy d-flex justify-content-between align-items-center">
            <h5 class="mb-0 fw-bold">
                Process Maintenance Request
            </h5>
            <small class="text-white-50">Ticket Number: @Model.TicketNumber</small>
        </div>

        <div class="card-body">
            <div class="p-3 bg-light border rounded mb-4 shadow-sm">
                <h6 class="fw-bold text-navy mb-3">Request Summary</h6>

                <dl class="row mb-0">
                    <dt class="col-sm-4 text-secondary">Tenant</dt>
                    <dd class="col-sm-8">@Model.TenantName</dd>

                    <dt class="col-sm-4 text-secondary">Unit</dt>
                    <dd class="col-sm-8">@Model.UnitName</dd>

                    <dt class="col-sm-4 text-secondary">Title</dt>
                    <dd class="col-sm-8">@Model.Title</dd>

                    <dt class="col-sm-4 text-secondary">Description</dt>
                    <dd class="col-sm-8">@(Model.Description ?? "No text description provided by tenant.")</dd>

                    <dt class="col-sm-4 text-secondary">Category</dt>
                    <dd class="col-sm-8">
                        <span class="badge bg-info-subtle text-info-emphasis">@Model.CategoryName</span>
                    </dd>

                    <dt class="col-sm-4 text-secondary">Priority</dt>
                    <dd class="col-sm-8">@Model.PriorityName</dd>

                    <dt class="col-sm-4 text-secondary">Current Status</dt>
                    <dd class="col-sm-8">@Model.CurrentStatusName</dd>

                    <dt class="col-sm-4 text-secondary">Assigned Staff</dt>
                    <dd class="col-sm-8">@Model.AssignedStaffName</dd>

                    <dt class="col-sm-4 text-secondary">Logged Timestamp</dt>
                    <dd class="col-sm-8">@Model.CreatedAt.ToString("dd MMM yyyy HH:mm")</dd>
                </dl>
            </div>

            @if (TempData["ManagementError"] != null)
            {
                <div class="alert alert-danger mb-4">
                    @TempData["ManagementError"]
                </div>
            }

            <form asp-area="Management"
                  asp-controller="MaintenanceManagement"
                  asp-action="ProcessTicket"
                  method="post">

                @Html.AntiForgeryToken()

                <input type="hidden" name="requestId" value="@Model.RequestId" />

                <div class="mb-4">
                    <label class="form-label fw-bold text-navy">
                        Update Maintenance Status
                    </label>

                    <select name="statusId"
                            class="form-select modern-input"
                            asp-items="ViewBag.StatusId"
                            required>
                        <option value="">-- Select Status --</option>
                    </select>
                </div>

                <div class="mb-4">
                    <label class="form-label fw-bold text-navy">
                        Assign Staff
                    </label>

                    <select name="staffId"
                            class="form-select modern-input mb-2"
                            asp-items="ViewBag.StaffId">
                        <option value="">-- Leave Unassigned --</option>
                    </select>

                    <small class="text-muted d-block">
                        Only available staff with the matching skill/category for
                        <strong>@Model.CategoryName</strong> are shown.
                    </small>
                </div>

                <div class="mb-4">
                    <label class="form-label fw-bold text-navy">
                        Manager Notes
                    </label>

                    <textarea name="managerNotes"
                              class="form-control modern-input"
                              rows="4"
                              placeholder="Optional note for staff/tenant, e.g. Assigned to technician for inspection."></textarea>
                </div>

                <div class="row pt-2">
                    <div class="col-6">
                        <a asp-area="Management"
                           asp-controller="MaintenanceManagement"
                           asp-action="Dashboard"
                           class="btn btn-secondary w-100">
                            Return to Dashboard
                        </a>
                    </div>

                    <div class="col-6">
                        <button type="submit" class="btn btn-navy w-100">
                            Save Changes
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>