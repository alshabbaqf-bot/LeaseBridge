@* <ul class="navbar-nav">
    @if (User.Identity?.IsAuthenticated ?? false)
    {
        <li class="nav-item">
            <span class="nav-link text-dark">
                Hello, @(User.Identity.Name ?? "user")!
            </span>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                @Html.AntiForgeryToken()
                <button type="submit" class="nav-link btn btn-link textdark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="" aspcontroller="Account" asp-action="Login">Login</a>
        </li>
    }
</ul> *@

@if (User.Identity?.IsAuthenticated ?? false)
{
    <div class="profile-section p-3 border-top border-secondary border-opacity-25 bg-dark bg-opacity-25">

        <!-- User Fullname -->
        <div class="d-flex align-items-center mb-3 px-2">

            <div class="bg-light rounded-circle d-flex align-items-center justify-content-center me-2"
                 style="width: 35px; height: 35px;">

                <i class="bi bi-person-fill text-navy"></i>
            </div>

            <div class="overflow-hidden">
                <p class="text-white small mb-0 fw-bold text-truncate">
                    @(User.Identity?.Name ?? "Manager User")
                </p>

                <small class="text-white-50">
                    Property Manager
                </small>
            </div>

        </div>

        <!-- Logout Button -->
        <form asp-controller="Account"
              asp-action="Logout"
              method="post">

            @Html.AntiForgeryToken()

            <button type="submit"
                    class="btn btn-sm btn-danger w-100 opacity-75">

                <i class="bi bi-box-arrow-left me-2"></i>
                Logout
            </button>

        </form>

    </div>
}



@* <!--Profile Section-->
            <div class="profile-section p-3 border-top border-secondary border-opacity-25 bg-dark bg-opacity-25">
                <!--User Fullname-->
                <div class="d-flex align-items-center mb-3 px-2">
                    <div class="bg-light rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 35px; height: 35px;">
                        <i class="bi bi-person-fill text-navy"></i>
                    </div>
                    <div class="overflow-hidden">
                        <p class="text-white small mb-0 fw-bold text-truncate">Manager User</p>
                    </div>
                </div>
                <!--Logout Button-->
                <form asp-controller="Account" asp-action="Logout" method="post">
                    <button type="submit" class="btn btn-sm btn-danger w-100 opacity-75 hover-opacity-100 transition">
                        <i class="bi bi-box-arrow-left me-2"></i> Logout
                    </button>
                </form>
            </div> *@